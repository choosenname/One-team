import { LoginForm } from "@/components/auth/login-form"; // Импорт компонента LoginForm из модуля компонентов аутентификации

// Компонент страницы входа в систему
const LoginPage = () => {
    return (
        <LoginForm /> // Вывод компонента LoginForm для отображения формы входа в систему
    );
}

export default LoginPage; // Экспорт компонента LoginPage
